import React from 'react';

const Main = () => {
  /*if (!token) {
  	
    return <Redirect to="/login" />;
  }
  console.log(JSON.parse(user));*/
  return <div> HomePage </div>;
};

export default Main;